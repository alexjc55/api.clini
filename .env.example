# ===========================================
# Waste Collection API - Environment Variables
# ===========================================

# Database connection (PostgreSQL)
# If not set, API uses in-memory storage (development only)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# JWT secret for authentication
# Minimum 32 characters, used to sign access and refresh tokens
# Generate with: openssl rand -base64 32
SESSION_SECRET=your_random_secret_minimum_32_characters

# Server configuration
NODE_ENV=development
PORT=5000

# CORS configuration
# Comma-separated list of allowed origins
# Leave empty or set to "*" to allow all origins (not recommended for production)
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ===========================================
# Notes
# ===========================================
# 
# DATABASE_URL format:
# postgresql://username:password@host:5432/database_name
#
# If password contains special characters, URL-encode them:
# # = %23, $ = %24, @ = %40, } = %7D, ~ = %7E
#
# For production:
# - Set NODE_ENV=production
# - Use strong SESSION_SECRET (32+ random characters)
# - Configure specific ALLOWED_ORIGINS (never use "*")
# - Use SSL for DATABASE_URL when required by your provider
